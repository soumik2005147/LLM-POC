# LLM Agent POC - Python Requirements
# 
# This project uses only Python standard library modules for the server.
# No external Python packages are required for the basic functionality.
#
# The server.py uses:
# - http.server (built-in)
# - socketserver (built-in) 
# - webbrowser (built-in)
# - os (built-in)
# - time (built-in)
# - urllib.parse (built-in)
# - threading (built-in)
#
# Frontend dependencies are managed via CDN links in HTML files:
# - Bootstrap 5.3.3
# - Bootstrap Icons 1.11.3
# - Marked.js for markdown rendering
# - Inter and JetBrains Mono fonts from Google Fonts

# If you need additional Python packages for development or extensions:
# requests>=2.28.0  # For HTTP requests if needed
# flask>=2.0.0      # Alternative web framework if needed
